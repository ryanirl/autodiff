import numpy as np
from autodiff.tensor import Tensor, OP
from autodiff.ops import grad_fun, value_fun
from autodiff.utils import primitive, check

# Convolutions
# MLP
# Layers
# Neuron? <- or is this to simple

# basically want functionallity like this:
# x.dot(l1).relu().dot(l2).relu().dot(l3).sigmoid()



